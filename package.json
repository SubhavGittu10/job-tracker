{
  "name": "job-tracker",
  "version": "1.0.0",
  "description": "Personal PM Job Search Intelligence System — Airtable + Claude AI + Vercel",
  "engines": { "node": "24.x" },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
